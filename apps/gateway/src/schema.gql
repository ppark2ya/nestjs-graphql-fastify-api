# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthToken {
  """JWT 액세스 토큰"""
  accessToken: String!

  """액세스 토큰 만료 시간 (초)"""
  expiresIn: Int!

  """리프레시 토큰"""
  refreshToken: String!
}

"""패스워드 변경 입력"""
input ChangePasswordInput {
  """현재 비밀번호"""
  currentPassword: String!

  """새 비밀번호: 8자 이상 영문/숫자/특수문자를 모두 포함하거나, 10자 이상 영문/숫자/특수문자 중 2종 이상을 포함"""
  newPassword: String!
}

"""Docker container information"""
type Container {
  """Unix timestamp of container creation"""
  created: Int!

  """Container ID (short format)"""
  id: String!

  """Image name"""
  image: String!

  """Container name"""
  name: String!

  """Swarm node hostname (null if not a swarm service)"""
  nodeName: String

  """Port mappings"""
  ports: [String!]!

  """Swarm service name (null if not a swarm service)"""
  serviceName: String

  """Container state (e.g., "running")"""
  state: String!

  """Container status (e.g., "Up 2 hours")"""
  status: String!

  """Swarm task ID (null if not a swarm service)"""
  taskSlot: String
}

"""Docker container resource usage statistics"""
type ContainerStats {
  """CPU usage percentage"""
  cpuPercent: Float!

  """Container ID (short format)"""
  id: String!

  """Memory limit in bytes"""
  memLimit: Float!

  """Memory usage in bytes"""
  memUsage: Float!

  """Container name"""
  name: String!
}

enum CurrencyCode {
  AUD
  CAD
  CNY
  EUR
  GBP
  HKD
  IDR
  JPY
  KRW
  MYR
  PHP
  SGD
  THB
  TWD
  USD
  VND
}

"""통화별 외화환전 가능 단말기 목록"""
type KioskAdminReleaseCurrencyTerminals {
  exchangeRate: Float!
  terminalInfos: [KioskAdminTerminalInfo!]!
}

"""키오스크 단말기 정보"""
type KioskAdminTerminalInfo {
  addr: String!
  detailAddr: String!
  lat: String!
  lon: String!
  operationDay: String!
  terminalNm: String!
  terminalNo: String!
}

"""로그 앱 정보"""
type LogApp {
  """앱 이름 (디렉토리명)"""
  name: String!

  """노드명"""
  node: String!
}

"""Container log entry"""
type LogEntry {
  """Container ID"""
  containerId: String!

  """Log message content"""
  message: String!

  """Output stream: stdout or stderr"""
  stream: String!

  """Log timestamp (RFC3339 format)"""
  timestamp: String!
}

"""파싱된 로그 라인"""
type LogLine {
  """로그 파일명"""
  file: String!

  """로그 레벨 (ERROR, WARN, INFO, DEBUG)"""
  level: String

  """파일 내 라인 번호"""
  lineNo: Int!

  """로그 메시지 (파싱 실패 시 원본 라인 전체)"""
  message: String!

  """추가 메타데이터 (JSON 문자열)"""
  metadata: String

  """Swarm 노드명"""
  node: String!

  """소스 (클래스명, 모듈명)"""
  source: String

  """타임스탬프 (파싱 불가 시 null)"""
  timestamp: String
}

"""로그 검색 입력"""
input LogSearchInput {
  """타임스탬프 커서 (페이지네이션)"""
  after: String

  """앱 이름"""
  app: String!

  """시작 날짜 (YYYY-MM-DD)"""
  from: String!

  """키워드 검색"""
  keyword: String

  """로그 레벨 필터"""
  level: String

  """결과 수 제한"""
  limit: Int = 100

  """노드 필터"""
  node: String

  """종료 날짜 (YYYY-MM-DD)"""
  to: String!
}

"""로그 검색 결과"""
type LogSearchResult {
  hasMore: Boolean!
  lines: [LogLine!]!
  summary: LogSummary!
}

"""로그 요약 통계"""
type LogSummary {
  errorCount: Int!
  fileCount: Int!
  infoCount: Int!
  totalLines: Int!
  warnCount: Int!
}

input LoginInput {
  """로그인 ID"""
  loginId: String!

  """비밀번호"""
  password: String!
}

type LoginResult {
  """2FA 인증 필요 여부"""
  requiresTwoFactor: Boolean!

  """Google OTP 등록 URL (등록 필요 시)"""
  tOtpUrl: String

  """로그인 성공 시 토큰 (2FA 불필요 시)"""
  tokens: AuthToken

  """2FA 임시 토큰 (2FA 필요 시)"""
  twoFactorToken: String
}

type Mutation {
  """패스워드 변경"""
  changePassword(input: ChangePasswordInput!): Boolean!

  """로그인"""
  login(input: LoginInput!): LoginResult!

  """토큰 갱신"""
  refreshToken(input: RefreshTokenInput!): AuthToken!

  """2FA TOTP 검증"""
  verifyTwoFactor(input: TotpVerifyInput!): AuthToken!
}

type Query {
  """Get resource usage statistics for specified containers"""
  containerStats(containerIds: [String!]!): [ContainerStats!]!

  """List all Docker containers"""
  containers: [Container!]!

  """통화별 외화환전 가능 단말기 목록 조회"""
  getReleaseCurrencyTerminals(currencyCode: CurrencyCode!): KioskAdminReleaseCurrencyTerminals!

  """로그 앱 목록 조회"""
  logApps: [LogApp!]!

  """로그 검색"""
  logSearch(input: LogSearchInput!): LogSearchResult!
}

input RefreshTokenInput {
  """리프레시 토큰"""
  refreshToken: String!
}

"""Service-level log entry (includes container lifecycle events)"""
type ServiceLogEntry {
  """Container ID that produced the log"""
  containerId: String!

  """
  Container lifecycle event type: container_started, container_stopped, or null for regular logs
  """
  event: String

  """Log message or event description"""
  message: String!

  """Swarm service name"""
  serviceName: String!

  """Output stream: stdout, stderr, or event"""
  stream: String!

  """Log timestamp (RFC3339 format)"""
  timestamp: String!
}

type Subscription {
  """Subscribe to container logs"""
  containerLog(containerId: String!): LogEntry!

  """
  Subscribe to all logs from a service (auto-recovers on container restart)
  """
  serviceLog(serviceName: String!): ServiceLogEntry!
}

input TotpVerifyInput {
  """TOTP 6자리 코드"""
  totpCode: String!
}